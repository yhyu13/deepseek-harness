# 知乎专栏粘贴说明

知乎不渲染 mermaid，也不显示 SVG。本文配图已是 PNG，按顺序在编辑器里上传后，再贴正文。

## 标题

让 Agent 盯住一个目标连跑几天，不要去改主循环

## 导语

给 Agent 加长期目标，不要改 loop。状态可回放，权限必须重授——这是能抄走的切割，不是文件名。

正文结构：标题下 TL;DR → 错路/脉络/树 → 收获和结论 → PS / PPS / PPPS。

## 配图上传顺序（12 张）

1. `images/09-wrong-loop.png` — 错路 / 正路
2. `images/00-thread.png` — 逻辑脉络
3. `images/02-tree.png` — 整棵树
4. `images/01-architecture.png` — 一个服务、三个消费者
5. `images/07-logged.png` — 模型看见的必须能翻出来
6. `images/08-two-stores.png` — 两种记账法
7. `images/06-cas.png` — 同时改一份账
8. `images/03-two-dimensions.png` — 走到哪 vs 准不准续
9. `images/04-authority.png` — 谁能改
10. `images/05-round-loop.png` — 一次一续
11. `images/10-evidence.png` — 证据先行
12. `images/11-takeaway.png` — 收获七条

正文里的 `![…](images/….png)` 粘贴后不会自动带上本地文件；在对应位置插入刚上传的图即可。

## 标签建议

Agent、LLM、系统设计、开源
